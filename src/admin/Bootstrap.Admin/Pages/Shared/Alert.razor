@inherits AlertBase

<Modal Backdrop="@Backdrop" IsCentered="@IsCentered" Size="@Size" Id="@Id" Title="@Title" ShowFooter="@ShowFooter">
    <ModalBody>
        @AlertBody
    </ModalBody>
    <ModalFooter>
        @AlertFooter
    </ModalFooter>
</Modal>

@code {

}
