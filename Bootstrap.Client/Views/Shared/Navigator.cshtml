@model NavigatorBarModel
<aside>
    <!-- sidebar menu start-->
    <ul class="sidebar">
        @foreach (var menu in Model.Navigations)
        {
            @await Html.PartialAsync("SubMenu", menu)
        }
    </ul>
    <!-- sidebar menu end-->
</aside>